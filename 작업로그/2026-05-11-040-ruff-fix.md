---
date: 2026-05-11
agent: backend-api-engineer
phase: 13
status: completed
roadmap_step: "040"
roadmap_impact: []
related_docs: []
---

# step 040 — ruff 64건 위반 수정 (ship-hold 해소)

## Plan

### 배경
Phase 13 test-engineer 🟡 ship-hold: ruff 64건 위반 (Phase 13 신규 파일에서 도입)
- app/ 14건: F401(미사용 import), I001(import 정렬), UP035/UP006/UP045(타입 힌트 현대화)
- tests/ 50건: E741(변수명 l), F401, I001, F541(빈 f-string), F841, B905

### 완료 기준
- ruff check backend/app backend/tests → 0 errors
- pytest backend/ → 1079건 이상 PASS, 0 FAIL

## Execution

### 자동 수정 (ruff --fix): 52건
`backend/.venv/Scripts/python.exe -m ruff check --fix backend/app backend/tests` 실행.
F401(미사용 import), I001(import 정렬), UP006/UP035/UP045(타입 힌트 현대화) 등 52건 자동 처리.

### 수동 수정 파일 목록

| 파일 | 위반 | 수정 내용 |
|------|------|-----------|
| `backend/tests/acceptance/test_accuracy_policy.py:276` | F841 | 미사용 `df = _make_df(...)` 할당 제거 |
| `backend/tests/api/test_error_catalog.py:228` | F841 | 미사용 `client = TestClient(app)` 할당 제거 |
| `backend/tests/api/test_trades_pagination.py:131` | F841 | 미사용 `total = r_all.json()["total_count"]` 제거 |
| `backend/tests/api/test_export.py:152,169,211` | E741 | `[l for l in ...]` → `[line for line in ...]` (3곳) |
| `backend/tests/exporters/test_csv_exporter.py:140,164,183,213,224,236,272,285,298,312,323` | E741 | `[l for l in ...]` → `[line for line in ...]` (replace_all, 10곳) |
| `backend/tests/exporters/test_csv_exporter.py:146` | B905 | `zip(headers, vals)` → `zip(headers, vals, strict=False)` |
| `backend/tests/integration/test_phase13_export_cancel_e2e.py:229` | B905 | `zip(r1.trade_executions, r2.trade_executions)` → `zip(..., strict=False)` |

총 수정: 자동 52건 + 수동 19건 = **71건**

## Tests

### ruff 최종 확인
```
backend/.venv/Scripts/python.exe -m ruff check backend/app backend/tests
→ All checks passed!  (0 errors)
```

### pytest 회귀
```
backend/.venv/Scripts/python.exe -m pytest backend/ -q --tb=short
→ 1079 passed, 10 warnings in 20.99s  (0 FAIL)
```

완료 기준 모두 달성:
- ruff check → 0 errors ✅
- pytest → 1079 PASS, 0 FAIL ✅

## Result

- 수정 파일 7개
- ruff 위반 71건 전량 해소 (자동 52 + 수동 19)
- pytest 1079건 PASS, 회귀 없음
- 적용 정책: E741(변수명 명확화), F841(미사용 변수), B905(zip strict=), 자동 수정 항목(F401/I001/UP계열)

## 메인 세션 마무리 체크
- [ ] status를 completed로 변경
- [ ] 작업로그/README.md 갱신
- [ ] git commit → git push origin main (Phase 13 완료 후 즉시)
